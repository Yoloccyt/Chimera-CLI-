# QA Report for v1.5.8-omega

**Date:** 2026-07-13
**Platform:** Windows 11 + bash, Rust stable-x86_64-pc-windows-gnu (project-local toolchain)
**Workspace version:** 1.5.8-omega
**Branch:** master

## Summary

| Check | Command | Status | Notes / Output summary |
|---|---|---|---|
| Format | `cargo fmt --all -- --check` | PASS | Exit code 0, no diff |
| Type check | `cargo check --workspace` | PASS | Exit code 0, finished in 3.40s |
| Clippy | `cargo clippy --workspace --all-targets --jobs 2 -- -D warnings` | PASS | Exit code 0, finished in 1m 35s |
| Tests (full workspace) | `cargo test --workspace` | PASS (retry) | First run: exit code 101 (linker failure on `kvbsr-router` lib test). Retry: exit code 0, 3,904 passed / 0 failed / 58 ignored |
| Focused tests | `cargo test -p chimera-cli` | PASS | Exit code 0, 5 proptests + 2 doc-tests passed |
| Focused E2E | `cargo test --test week8_final_acceptance` | PASS | Exit code 0, 12 tests passed |
| Security audit | `cargo audit --no-fetch --deny warnings --ignore RUSTSEC-2026-0190 --ignore RUSTSEC-2026-0002 --ignore RUSTSEC-2024-0436` | PASS | Loaded 1,160 advisories, scanned 385 dependencies, exit code 0, no vulnerabilities. Live fetch blocked by network; see Security Report |
| Fuzz static check | `cargo check --manifest-path fuzz/Cargo.toml` | PASS | Exit code 0, finished in 14.61s |
| Fuzz config static check | `scripts/check_fuzz_config.ps1` | PASS | Exit code 0, all 6 stages PASS (8 fuzz targets + 4 member crates verified) |
| Release build | `cargo build --workspace --release` | PASS | Exit code 0, finished in 40.62s |
| Binary size | `ls -lh target/release/aether.exe` | PASS | 1.4 MB, well under 50 MB limit |
| Binary version | `target/release/aether.exe --version` | PASS | `aether 1.5.8-omega` |
| Release-mode ignored tests | `cargo test --workspace --release -- --ignored --nocapture` | PASS_WITH_WARNINGS | Full run: `mlc-engine` `test_l0_lru_eviction_o1_performance` failed (p99 239700ns vs 100000ns) on Windows high-load environment. Isolated rerun: `cargo test --release -p mlc-engine --test working_memory -- --ignored --nocapture test_l0_lru_eviction_o1_performance` passed. Failure attributed to parallel workspace build/test load. Log: `.superpowers/sdd/release-ignored-tests.log` |
| Docker verification | CI `release.yml` docker job | PASS | Docker image built and pushed successfully for `v1.5.8-omega`; local Podman failure was environment-specific. Run: https://github.com/Yoloccyt/Chimera-CLI-/actions/runs/29239575906 |

## Environment used for all cargo commands

```bash
export CARGO_HOME='/d/Chimera CLI/.toolchain/cargo'
export RUSTUP_HOME='/d/Chimera CLI/.toolchain/rustup'
export TMP='/d/Chimera CLI/tmp'
export TEMP='/d/Chimera CLI/tmp'
export PATH="/d/Chimera CLI/.toolchain/cargo/bin:/d/msys64/mingw64/bin:$PATH"
export RUST_MIN_STACK=33554432
# Clippy additionally set CARGO_INCREMENTAL=0
```

## Detailed logs

All raw logs are preserved under `.superpowers/sdd/logs/`:

- `01_fmt.log`
- `02_check.log`
- `03_clippy.log`
- `04_test.log` (first failed run)
- `04_test_retry.log` (successful retry)
- `04_test_chimera_cli.log`
- `04_test_week8.log`
- `04_test_kvbsr.log`
- `05_audit.log`
- `06_fuzz_check.log`
- `07_fuzz_config.log`
- `08_release_build.log`
- `08_binary_size.log`
- `08_binary_version.log`
- `09_docker_version.log`
- `09_docker_verify.log`

## Defect list

| # | Severity | Finding | Recommended action |
|---|---|---|---|
| 1 | Medium | Full workspace test first run failed with GCC linker error on `kvbsr-router` (exit code 1, no detailed linker message). Retry passed; isolated `cargo test -p kvbsr-router` also passed. Likely transient resource/linker contention under parallel workspace build. | Monitor CI behavior; if reproducible, investigate MinGW linker / parallel job limits. Consider running release tests with `--jobs 1` or increasing system resources. |
| 2 | Low | Initial live `cargo audit` fetch failed due to network restrictions. Retry with `--no-fetch` passed (1,160 advisories, 385 dependencies, no findings). | Confirm CI audit job (`.github/workflows/audit.yml`) is green before release, since it has unrestricted network access. |
| 4 | High (transient) | Release-mode ignored tests: full workspace run failed `mlc-engine` `test_l0_lru_eviction_o1_performance` with p99 latency 239700ns vs 100000ns on Windows high-load environment. Isolated rerun of the same test passed. | Rerun in CI (Linux runner, `--jobs 1`) to confirm environment-specific tail latency; if CI passes, waive local failure. If CI also fails, investigate threshold suitability or optimize LRU eviction path. |
| 5 | Low | Docker Desktop not installed locally; Podman build failed with `archive/tar: write too long`. CI `release.yml` docker job completed successfully for `v1.5.8-omega`. | N/A — CI validated the image build. |

## Overall QA verdict

**PASS — v1.5.8-omega release pipeline completed successfully in CI**

All code-quality checks (fmt, check, clippy) pass. The full workspace test suite passes on retry with 3,904 tests passing and zero failures. Release-mode ignored tests were executed locally; one performance test (`mlc-engine` `test_l0_lru_eviction_o1_performance`) failed only in the Windows high-load full-workspace run, but passed in isolation, indicating transient parallel-load noise. CI `release.yml` run [29239575906](https://github.com/Yoloccyt/Chimera-CLI-/actions/runs/29239575906) completed successfully: all 5 platform builds, Ubuntu tests, Docker image build, .deb package build, and GitHub Release publication are green. Release assets are available at https://github.com/Yoloccyt/Chimera-CLI-/releases/tag/v1.5.8-omega.

## Sign-off

- **QA Lead:** ___________________ Date: __________
- **Tech Lead:** ___________________ Date: __________
