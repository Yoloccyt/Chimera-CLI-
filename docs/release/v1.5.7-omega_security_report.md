# Security Report for v1.5.7-omega

**Date:** 2026-07-13
**Platform:** Windows 11 + bash, Rust stable-x86_64-pc-windows-gnu (project-local toolchain)
**Workspace version:** 1.5.7-omega

## Summary

| Area | Status | Notes |
|---|---|---|
| `cargo audit` CVE scan | PASS | `cargo audit --no-fetch` loaded 1,160 advisories and scanned 385 Cargo.lock dependencies with exit code 0; no vulnerabilities found. The live fetch failed due to network restrictions, but a cached advisory DB was available. |
| Fuzz crate static check | PASS | `cargo check --manifest-path fuzz/Cargo.toml` exit code 0. |
| Fuzz config static check | PASS | `scripts/check_fuzz_config.ps1` exit code 0; 8 targets + 4 member crates verified. |
| `#![forbid(unsafe_code)]` | PASS | All 35 workspace crate `src/lib.rs` files declare `#![forbid(unsafe_code)]`. `chimera-cli/src/main.rs` also declares it. |
| Release binary | PASS | `target/release/aether.exe` version `aether 1.5.7-omega`, size 1.4 MB (< 50 MB). |
| Dockerfile security baseline | PASS | Static inspection confirms distroless runtime, `nonroot` user, exec-form `ENTRYPOINT`, `HEALTHCHECK`, `ARG VERSION`, and OCI labels. |
| Container image build | DEGRADED | Docker unavailable locally; Podman build failed with `archive/tar: write too long`. Full image validation must be confirmed in CI. |

## Audit results

**Command:**

```bash
cargo audit --deny warnings \
  --ignore RUSTSEC-2026-0190 \
  --ignore RUSTSEC-2026-0002 \
  --ignore RUSTSEC-2024-0436
```

**Initial attempt (live fetch):**

- **Exit code:** 1
- **Output:** Could not fetch `https://github.com/RustSec/advisory-db.git` due to network/IO error.

**Retry with cached advisory DB:**

- **Command:** `cargo audit --no-fetch --deny warnings --ignore RUSTSEC-2026-0190 --ignore RUSTSEC-2026-0002 --ignore RUSTSEC-2024-0436`
- **Exit code:** 0
- **Output:**
  ```text
  Loaded 1160 security advisories
  Scanning Cargo.lock for vulnerabilities (385 crate dependencies)
  ```
- **Assessment:** No vulnerable packages found. The three expected ignores (`RUSTSEC-2026-0190`, `RUSTSEC-2026-0002`, `RUSTSEC-2024-0436`) were supplied on the command line. Because the live fetch is blocked in this environment, the final release should also confirm the daily CI audit job (`.github/workflows/audit.yml`) is green.

## Fuzz status

### Fuzz crate static check

- **Command:** `cargo check --manifest-path fuzz/Cargo.toml`
- **Exit code:** 0
- **Summary:** All fuzz crate dependencies resolve and compile; no type errors.

### Fuzz config static check

- **Command:** `powershell -ExecutionPolicy Bypass -File scripts/check_fuzz_config.ps1`
- **Exit code:** 0
- **Summary:** The script verified:
  1. `fuzz/Cargo.toml` exists and parses.
  2. `[package.metadata] cargo-fuzz = true` is present.
  3. `[lib] path = src/lib.rs` is configured and the file exists.
  4. All 8 `[[bin]]` targets have corresponding source files.
  5. All 8 fuzz target files contain the `fuzz_target!` macro invocation.
  6. All 4 member crate paths referenced in `fuzz/Cargo.toml` exist.

Note: The script printed PowerShell profile warnings (`Terminal-Icons` module not found, `Set-PSReadLineOption` unsupported in redirected console); these are environmental and do not affect validation results.

## `#![forbid(unsafe_code)]` status

All 35 workspace crates declare `#![forbid(unsafe_code)]` at the top of `src/lib.rs`:

```text
crates/acb-governor/src/lib.rs
crates/auto-dpo/src/lib.rs
crates/chimera-cli/src/lib.rs
crates/chimera-tui/src/lib.rs
crates/chtc-bridge/src/lib.rs
crates/cmt-tiering/src/lib.rs
crates/csn-substitutor/src/lib.rs
crates/decay-engine/src/lib.rs
crates/decb-governor/src/lib.rs
crates/efficiency-monitor/src/lib.rs
crates/event-bus/src/lib.rs
crates/faae-router/src/lib.rs
crates/gea-activator/src/lib.rs
crates/gqep-executor/src/lib.rs
crates/gsoe-evolution/src/lib.rs
crates/hcw-window/src/lib.rs
crates/kvbsr-router/src/lib.rs
crates/lsct-tiering/src/lib.rs
crates/mlc-engine/src/lib.rs
crates/mcp-mesh/src/lib.rs
crates/model-router/src/lib.rs
crates/mtpe-executor/src/lib.rs
crates/nexus-core/src/lib.rs
crates/nmc-encoder/src/lib.rs
crates/online-learning/src/lib.rs
crates/osa-coordinator/src/lib.rs
crates/parliament/src/lib.rs
crates/pvl-layer/src/lib.rs
crates/qeep-protocol/src/lib.rs
crates/repo-wiki/src/lib.rs
crates/scc-cache/src/lib.rs
crates/sesa-router/src/lib.rs
crates/seccore/src/lib.rs
crates/ssra-fusion/src/lib.rs
crates/quest-engine/src/lib.rs
```

Additionally, `crates/chimera-cli/src/main.rs` also declares `#![forbid(unsafe_code)]`.

No crate in the workspace is missing the directive at its library/binary top level.

## Dockerfile security baseline

Static inspection of `Dockerfile` confirms the following security-critical constructs are present:

| Requirement | Status | Evidence |
|---|---|---|
| Multi-stage build with builder image | PASS | `FROM rust:1.85-slim AS builder` |
| Minimal runtime base image | PASS | `FROM gcr.io/distroless/cc-debian12` |
| COPY from builder stage | PASS | `COPY --from=builder ...` |
| Non-root file ownership | PASS | `--chown=nonroot:nonroot` |
| Non-root runtime user | PASS | `USER nonroot:nonroot` |
| Exec-form entrypoint (no shell) | PASS | `ENTRYPOINT ["chimela"]` |
| Health check | PASS | `HEALTHCHECK --interval=30s ... CMD ["chimela", "--version"]` |
| Version build arg | PASS | `ARG VERSION=1.5.7-omega` |
| Backtrace enabled for diagnostics | PASS | `ENV RUST_BACKTRACE=1` |
| OCI image labels | PASS | `LABEL org.opencontainers.image.title=...` |

## Container image build

- **Docker CLI:** Not found (`docker: command not found`).
- **Podman CLI:** Found, but `podman build` failed with `archive/tar: write too long` (exit code 125). This is a Podman runtime/storage issue, not a Dockerfile issue.
- **Release binary proxy check:** `target/release/aether.exe` is 1.4 MB, satisfying the < 50 MB gate that is a necessary condition for the < 100 MB image target.
- **Recommendation:** Complete image validation in CI via the `release.yml` docker job after pushing the release tag.

## Defects / concerns

| # | Severity | Finding | Recommended action |
|---|---|---|---|
| 1 | Low | Initial `cargo audit` live fetch failed due to network restrictions. Retry with `--no-fetch` using the cached advisory DB passed with no vulnerabilities. | Confirm the daily CI audit job (`.github/workflows/audit.yml`) is green before release, as it has unrestricted network access. |
| 2 | Low | Local Podman image build failed; Docker Desktop is not installed. | Rely on CI `release.yml` docker job for authoritative image build, `--version`, and size validation. |

## Overall security verdict

**PASS_WITH_WARNINGS**

The codebase maintains its `#![forbid(unsafe_code)]` invariant across all 35 workspace crates, the Dockerfile security baseline is intact, fuzz static checks pass, and `cargo audit --no-fetch` found no vulnerabilities among 385 dependencies. The remaining warning is environmental: the local container engine could not build the image, so full image validation must be confirmed by CI.

## Sign-off

- **Security Lead:** ___________________ Date: __________
- **Tech Lead:** ___________________ Date: __________
