# Release Approval Checklist for v1.5.7-omega

**Date:** 2026-07-13
**Release:** v1.5.7-omega
**Branch:** master

This checklist follows the 14 pre-release verification items defined in `.claude/CLAUDE.md` §5.

| # | Item | Status | Evidence / Notes |
|---|---|---|---|
| 1 | `Cargo.toml` `workspace.package.version` matches release tag `1.5.7-omega` | ✅ | `Cargo.toml` line 31: `version = "1.5.7-omega"` |
| 2 | `CHANGELOG.md` contains a `v1.5.7-omega` summary section | ✅ | `CHANGELOG.md` line 12 begins the `v1.5.7-omega` summary |
| 3 | `cargo check --workspace` passes | ✅ | Exit code 0; finished in 3.40s. Log: `.superpowers/sdd/logs/02_check.log` |
| 4 | `cargo clippy --workspace --all-targets --jobs 2 -- -D warnings` passes | ✅ | Exit code 0; finished in 1m 35s. Log: `.superpowers/sdd/logs/03_clippy.log` |
| 5 | `cargo fmt --all -- --check` passes | ✅ | Exit code 0; no formatting diff. Log: `.superpowers/sdd/logs/01_fmt.log` |
| 6 | `cargo test --workspace` passes | ✅ | Retry run: exit code 0, 3,904 passed / 0 failed / 58 ignored. First run experienced a transient linker error on `kvbsr-router`. Log: `.superpowers/sdd/logs/04_test_retry.log` |
| 7 | `cargo test --workspace --release -- --ignored --nocapture` (perf/stress thresholds) | ✅ (with note) | Full run: one performance test `mlc-engine::test_l0_lru_eviction_o1_performance` failed with p99 239700ns vs 100000ns on Windows high-load environment. Isolated rerun of the same test passed, indicating transient parallel-load noise. Log: `.superpowers/sdd/release-ignored-tests.log`. CI run is the final authority. |
| 8 | `cargo audit --deny warnings --ignore RUSTSEC-2026-0190 --ignore RUSTSEC-2026-0002 --ignore RUSTSEC-2024-0436` | ✅ | `cargo audit --no-fetch` loaded 1,160 advisories, scanned 385 Cargo.lock dependencies, exit code 0, no vulnerabilities found. Live fetch is blocked in this environment; confirm CI audit job is green before release. Logs: `.superpowers/sdd/logs/05_audit.log`, `.superpowers/sdd/release-audit-retry.log` |
| 9 | `cargo check --manifest-path fuzz/Cargo.toml` + `scripts/check_fuzz_config.ps1` | ✅ | Fuzz crate check exit code 0. Fuzz config script exit code 0; all 8 targets + 4 member crates verified. Logs: `.superpowers/sdd/logs/06_fuzz_check.log`, `.superpowers/sdd/logs/07_fuzz_config.log` |
| 10 | `docker build -t chimera-cli:rc .` | ⏸️ | Docker Desktop not installed locally. Podman attempted build but failed with `archive/tar: write too long`. Full image build must be verified by CI `release.yml` docker job after tag push. Log: `.superpowers/sdd/logs/09_docker_verify.log` |
| 11 | `docker run --rm chimera-cli:rc --version` matches `^(aether\|chimera\|chimela) [0-9]+\.[0-9]+\.[0-9]+` | ⏸️ | Depends on item 10. Release binary locally reports `aether 1.5.7-omega`. Log: `.superpowers/sdd/logs/08_binary_version.log` |
| 12 | `docker image inspect chimera-cli:rc --format '{{.Size}}'` confirms image < 100 MB | ⏸️ | Depends on item 10. Release binary is 1.4 MB, satisfying the necessary condition for the < 100 MB image target. Log: `.superpowers/sdd/logs/08_binary_size.log` |
| 13 | `cargo build --workspace --release` confirms binary < 50 MB | ✅ | Exit code 0; `target/release/aether.exe` is 1.4 MB. Logs: `.superpowers/sdd/logs/08_release_build.log`, `.superpowers/sdd/logs/08_binary_size.log` |
| 14 | `git tag v1.5.7-omega && git push origin v1.5.7-omega` | ⏸️ | Release action; intentionally not executed during verification. To be performed by release owner after all checklist items are green. |

## Blocking items before tag push

The following items should be confirmed green before the release tag is pushed:

1. **Item 7** — The `mlc-engine` release-mode ignored test `test_l0_lru_eviction_o1_performance` failed only in the local Windows high-load full-workspace run; the same test passed in isolation. Rerun/confirm in CI (Linux runner, `--jobs 1`) and waive if CI passes. Log: `.superpowers/sdd/release-ignored-tests.log`.
2. **Items 10-12** — Confirm the CI `release.yml` docker job builds the image, `docker run --rm <image> --version` matches the expected pattern, and the image size is < 100 MB.

Item 8 (`cargo audit`) passed locally with `--no-fetch`; confirm the CI audit job is green as the authoritative network-fresh scan.

## Non-blocking notes

- The first `cargo test --workspace` run failed with a MinGW linker error on `kvbsr-router`. A retry passed, and the crate passed in isolation. This appears to be transient parallel-build contention rather than a code defect, but it should be monitored in CI.
- The local Podman build failure (`archive/tar: write too long`) is a container-engine storage issue, not a Dockerfile or binary issue.
- No `check_errors*.txt` files were left in the repository root.

## Sign-off placeholders

| Role | Name | Signature | Date | Decision |
|---|---|---|---|---|
| Tech Lead | | | | ⬜ Approve / ⬜ Reject / ⬜ Approve with conditions |
| Product Lead | | | | ⬜ Approve / ⬜ Reject / ⬜ Approve with conditions |
| Security Lead | | | | ⬜ Approve / ⬜ Reject / ⬜ Approve with conditions |

## Release-readiness summary

**Current state: CONDITIONAL GO — CONFIRM CI PERFORMANCE RUN AND DOCKER JOB**

Code-quality gates (fmt, check, clippy, tests, release build, binary size) pass. The `cargo test --workspace --release -- --ignored --nocapture` full run showed a transient `mlc-engine` p99 latency failure (239700ns vs 100000ns) in the local Windows high-load environment, but the same test passed in isolation, strongly suggesting environment noise rather than a regression. The remaining work is environmental/CI confirmation: rerun the performance test in CI and validate the container image build via `release.yml` docker job. Once those are green, the release is approved for tagging.
