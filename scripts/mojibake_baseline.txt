# mojibake 债务基线（check_doc_consistency.ps1 的 D5 检查消费本文件）
#
# 格式：<允许的最大损伤行数> TAB <文件路径>
#   * 未列出的受检文件：上限为 0（新文件必须干净）
#   * 实际损伤 > 基线   ：[GAP-D5] 红
#   * 实际损伤 < 基线   ：[D5-INFO] 提示把基线改小（棘轮只收不放）
#
# 损伤判据（不是"罕见字"本身，而是罕见字紧跟 `?`，或 U+FFFD 替换字）：
# UTF-8 被按 GBK 解码时，多字节序列的尾字节会变成不可表示字符 `?`，
# 因此 `锛?` `往杩?` 这类形态是硬证据。首版扫描把常用字 `存` 放进判据，
# "存储/保存"造成上千假阳，已修正（教训：判据本身要过负对照）。
#
# 来源：2026-08-31 Phase R 清点实测。CHANGELOG.md 1191 行中 94 行损伤，
# 集中在 Milestone-B/Phase 5 时代条目；其余五份核心文档实测 0 行。
# 修复方式必须是"逐行回到代码/ADR 核实后重写"，禁止按字形猜（见 RK-P40）。
94	CHANGELOG.md
